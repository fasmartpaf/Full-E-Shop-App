# Testing App Brief

FlutterGo starter project (Android, iOS, Web).

```bash
flutter pub get
flutter run -d chrome
```

Edit Dart under `lib/`. Platform config lives in `android/`, `ios/`, and `web/`.

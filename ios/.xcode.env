# Environment variables for Xcode build scripts (versioned).
# Local overrides belong in `.xcode.env.local` (not versioned).

# Avoid "database is locked / concurrent builds" errors in preview runs.
export FLUTTER_XCODE_BUILD_PARALLEL=false
